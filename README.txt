

# The sdcard.py is the library to read the sd card connected the esp32 board
# sd_esp32connection is a python file that will mount the SD card and read data from the SD card folders 
that have the code and data to implement the python code for training a machine learning model


The SD card Pin connection to esp32
sck=Pin(18), mosi=Pin(23), miso=Pin(19)),(cs, Pin(5))